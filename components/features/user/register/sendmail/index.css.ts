import { style } from '@vanilla-extract/css';

const wrapper = style({
	display: 'flex',
	flexDirection: 'column',
	marginTop: '108px',
	padding: '0 16px',
});

const styles = {
	wrapper,
};

export default styles;
